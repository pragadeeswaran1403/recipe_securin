# recipe_securin
This project is a Recipe Management REST API built using Java and Spring Boot. It provides endpoints to fetch, search, and filter recipes with support for pagination and dynamic query parameters using Spring Data JPA.
